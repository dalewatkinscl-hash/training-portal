{
  "name": "training",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "deploy": "npm run build && firebase deploy --only hosting,functions,firestore",
    "deploy:hosting": "npm run build && firebase deploy --only hosting"
  }
}
