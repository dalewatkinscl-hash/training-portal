rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All client access goes through Cloud Functions + Employee Portal SSO.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
